!!! 5
%h1 ログイン画面
= form_for @user,:utl => {:action => 'preview'}  do |f|
  - if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :pass
    = f.text_field :pass
  .actions
    = f.submit 'submit'
